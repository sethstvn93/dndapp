'use strict'

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  main: {
    flex: 1,
    backgroundColor: '#444',
    justifyContent: 'space-between'
  },
  container: {
    flex: 1,
    flexDirection: 'row'
  }
});
